if (Meteor.isClient) {
  var id = 0;
  Template.hello.rendered = function () {
    var switchReactiveVar = new ReactiveVar(true);

    for (var g = 0; g < 7; g++) {
      for (var i = 0; i < 10; i++) {
        switchSetup(false, function (switchState) {
        }, 'switchStub-' + g, switchReactiveVar); //, generatePalette(i));
      }
    }

    Meteor.setInterval(function () {
      switchReactiveVar.set(!switchReactiveVar.get());
    }, 250);
  };

  Template.hello.helpers({
    stubId: function () {
      return id++;      
    }
  });
}